// This file previously contained a standalone ReactActivity used by the library during
// development. Libraries must not provide launcher activities; the host application
// provides the app entrypoint. Keep this file as a stub to avoid accidental inclusion.

package com.vonagevoicecall

/** MainActivity intentionally left blank in the library distribution. */
class MainActivity {
  // No-op: host application should supply its own ReactActivity.
}
